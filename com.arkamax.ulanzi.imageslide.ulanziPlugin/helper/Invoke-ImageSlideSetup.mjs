#!/usr/bin/env node
import { createHash, randomUUID } from "node:crypto";
import { cp, mkdir, readFile, rename, rm, writeFile } from "node:fs/promises";
import { basename, dirname, join, resolve, relative } from "node:path";
import { homedir } from "node:os";
import { execFile as execFileCallback } from "node:child_process";
import { promisify } from "node:util";
import { pathToFileURL } from "node:url";

const execFile=promisify(execFileCallback);
const ACTION="com.arkamax.ulanzi.imageslide.slideshow", SETUP="com.arkamax.ulanzi.imageslide.setup", BUILTIN="com.ulanzi.ulanzideck.smallwindow.window", VERSION="0.5.1";
const CODES=new Set(["PROFILE_NOT_FOUND","PROFILE_AMBIGUOUS","SETUP_INSTANCE_NOT_FOUND","PAGE_INVALID","SLOT_UNRELATED","SETTINGS_SCHEMA_UNSUPPORTED","REQUEST_WRITE_FAILED","PROFILE_STORE_UNREADABLE","MANIFEST_INVALID","COMPATIBILITY_UNSUPPORTED","HELPER_PROCESS_FAILED","REPREPARE_REQUIRED","RESTORE_BACKUP_NOT_FOUND","RESTORE_BACKUP_INVALID","RESTORED"]);
const PHASES=new Set(["INITIALIZING","COMPATIBILITY","COMPAT_PLUGIN_ROOT","COMPAT_MANIFEST_READ","COMPAT_EXE_PATH","COMPAT_VERSION_READ","COMPAT_HASH_READ","COMPAT_ENV_PATHS","SETTINGS_READ","SETTINGS_SCHEMA","V2_ENUMERATION","DEVICE_PROFILE_MATCH","PAGE_READ","TARGET_RESOLUTION","RESTORE_RESOLUTION","SLOT_VALIDATION","REQUEST_WRITE","APPLY_PRECHECK","BACKUP","PATCH_WRITE","RESTORE_WRITE","READBACK","RECEIPT","MANUAL_REOPEN"]);
const hash=value=>createHash("sha256").update(value).digest("hex");
const fileHash=async path=>hash(await readFile(path));
const safeSegment=value=>typeof value==="string"&&/^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/.test(value)&&value!=="."&&value!=="..";
const uuid=value=>typeof value==="string"&&/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(value);
function fail(code,phase){const error=new Error(code);error.code=CODES.has(code)?code:"HELPER_PROCESS_FAILED";error.phase=PHASES.has(phase)?phase:"INITIALIZING";throw error}
function under(root,...parts){const path=resolve(root,...parts);if(relative(resolve(root),path).startsWith(".."))fail("HELPER_PROCESS_FAILED","INITIALIZING");return path}
async function json(path,code,phase){try{return JSON.parse(await readFile(path,"utf8"))}catch{fail(code,phase)}}
function large(document,code="PAGE_INVALID",phase="SLOT_VALIDATION"){const found=(document.Controllers||[]).filter(c=>c?.Type==="Keypad"&&c.Actions?.["3_2"]);if(found.length!==1)fail(code,phase);return found[0]}
function setupController(document,key,actionHash){const found=(document.Controllers||[]).filter(c=>c?.Type==="Keypad"&&c.Actions?.[key]);if(found.length!==1)fail("SETUP_INSTANCE_NOT_FOUND","TARGET_RESOLUTION");const entry=found[0].Actions[key];if(entry?.Action!==SETUP||!uuid(entry.ActionID)||hash(entry.ActionID.toLowerCase())!==actionHash)fail("SETUP_INSTANCE_NOT_FOUND","TARGET_RESOLUTION")}
function canonical(value){if(Array.isArray(value))return value.map(canonical);if(value&&typeof value==="object")return Object.fromEntries(Object.keys(value).sort().map(key=>[key,canonical(value[key])]));return value}
function canonicalText(value){return JSON.stringify(canonical(value))}
function generatedCenter(actionId){return{Action:ACTION,ActionID:actionId,ActionParam:{SmallViewMode:2},LinkedTitle:true,Name:"Image Slideshow",Plugin:{Name:"Image Slideshow",UUID:"com.arkamax.ulanzi.imageslide",Version:VERSION},State:0,ViewParam:[{Icon:"",IconRel:"",Name:"Image Slideshow"}]}}
function normalizedCenter(action){if(!uuid(action?.ActionID))return null;const expected=generatedCenter(action.ActionID),plugin=action.Plugin;if(canonicalText(plugin)!==canonicalText(expected.Plugin)&&canonicalText(plugin)!=="{}")return null;const normalized=structuredClone(action);normalized.Plugin={};const normalizedExpected=structuredClone(expected);normalizedExpected.Plugin={};return canonicalText(normalized)===canonicalText(normalizedExpected)?normalized:null}
function centerFingerprint(action){const normalized=normalizedCenter(action);return normalized?hash(canonicalText(normalized)):null}
function normalizedPatchedPage(document,setupKey){const copy=structuredClone(document),center=large(copy,"RESTORE_BACKUP_INVALID","RESTORE_RESOLUTION").Actions["3_2"];if(!normalizedCenter(center))return null;center.Plugin={};const setupEntries=(copy.Controllers||[]).filter(controller=>controller?.Type==="Keypad"&&controller.Actions?.[setupKey]);if(setupEntries.length!==1||setupEntries[0].Actions[setupKey]?.Action!==SETUP)return null;const setupAction=setupEntries[0].Actions[setupKey],params=setupAction.ActionParam;if(params&&Object.prototype.hasOwnProperty.call(params,"operation")){if(!["install","restore"].includes(params.operation))return null;delete params.operation}if(params&&typeof params==="object"&&!Array.isArray(params)&&Object.keys(params).length===0)delete setupAction.ActionParam;return canonicalText(copy)}
function receiptBound(receipt,target){return receipt?.target?.store===target.store&&receipt.target.groupId===target.groupId&&receipt.target.pageId===target.pageId&&receipt.target.key==="3_2"}
async function studioClosed(){try{await execFile("pgrep",["-x","UlanziDeck"]);return false}catch(error){return error.code===1}}
export async function validateStudioIdentity(pluginRoot,{bundleIdentifier}={}){const compatibility=await json(under(pluginRoot,"helper","compatibility.json"),"COMPATIBILITY_UNSUPPORTED","COMPAT_MANIFEST_READ");const allowed=compatibility?.studio?.macos?.bundleIdentifiers;if(compatibility?.schema!=="com.arkamax.ulanzi.imageslide.compatibility/v1"||!Array.isArray(allowed)||!allowed.includes(bundleIdentifier))fail("COMPATIBILITY_UNSUPPORTED","COMPATIBILITY")}
async function atomicWrite(path,text){const temp=join(dirname(path),`.imageslide-${randomUUID()}.tmp`);try{await writeFile(temp,text,"utf8");await rename(temp,path)}catch(error){await rm(temp,{force:true});throw error}}
let statusBinding={};
async function writeStatus(stateRoot,status,code,phase,handshakeId){if(phase==="RELAUNCH")phase="MANUAL_REOPEN";await mkdir(stateRoot,{recursive:true});await atomicWrite(under(stateRoot,"last-diagnostic.json"),JSON.stringify({status,code,phase,handshakeId,...statusBinding}))}
function targetKey(target){return [target.store,target.groupId,target.pageId,target.manifest].join("\0")}

export async function resolveTarget({dataRoot,key,setupActionId}){
  if(!/^\d{1,2}_\d{1,2}$/.test(key)||key==="3_2"||!uuid(setupActionId))fail("SETUP_INSTANCE_NOT_FOUND","TARGET_RESOLUTION");
  const state=await json(under(dataRoot,"Config","setting_source.json"),"SETTINGS_SCHEMA_UNSUPPORTED","SETTINGS_READ");
  if(!Array.isArray(state.Devices)||state.Devices.length===0)fail("SETTINGS_SCHEMA_UNSUPPORTED","SETTINGS_SCHEMA");
  const matches=[]; const actionHash=hash(setupActionId.toLowerCase());
  for(const device of state.Devices){if(typeof device?.CurrentProfile!=="string"||typeof device?.CurrentDevice!=="string")continue;
    const store=under(dataRoot,"ProfilesV2"); let groups=[];try{groups=await (await import("node:fs/promises")).readdir(store,{withFileTypes:true})}catch{continue}
    for(const group of groups.filter(g=>g.isDirectory()&&safeSegment(g.name))){const root=under(store,group.name), rootManifest=under(root,"manifest.json");let profile;try{profile=await json(rootManifest,"MANIFEST_INVALID","DEVICE_PROFILE_MATCH")}catch{continue}
      if(profile?.Name!==device.CurrentProfile||profile?.Device?.UUID!==device.CurrentDevice||profile?.Device?.Model!=="D200"||!safeSegment(profile?.Pages?.Current)||!Array.isArray(profile.Pages.Pages)||!profile.Pages.Pages.includes(profile.Pages.Current))continue;
      const pageId=profile.Pages.Current, manifest=under(root,"Profiles",pageId,"manifest.json");let page;try{page=await json(manifest,"MANIFEST_INVALID","PAGE_READ")}catch{fail("PAGE_INVALID","PAGE_READ")}
      setupController(page,key,actionHash); const actions=large(page).Actions, slot=actions["3_2"]; if(!slot?.Action)fail("PAGE_INVALID","SLOT_VALIDATION");
      if(slot.Action!==BUILTIN&&slot.Action!==ACTION)fail("SLOT_UNRELATED","SLOT_VALIDATION");
      matches.push({store:"ProfilesV2",groupId:group.name,pageId,manifest,rootManifest,profileName:profile.Name,deviceUuid:profile.Device.UUID,setupKey:key,setupActionIdSha256:actionHash,kind:slot.Action===BUILTIN?"patch":"restore"});
    }}
  const unique=[...new Map(matches.map(target=>[targetKey(target),target])).values()]; if(unique.length===0)fail("SETUP_INSTANCE_NOT_FOUND","TARGET_RESOLUTION");if(unique.length!==1)fail("PROFILE_AMBIGUOUS","TARGET_RESOLUTION");return unique[0];
}
export async function prepare({dataRoot,stateRoot,key,setupActionId,operation="install"}){
  const target=await resolveTarget({dataRoot,key,setupActionId});if((operation==="restore")!== (target.kind==="restore"))fail("SLOT_UNRELATED","SLOT_VALIDATION");
  const now=new Date(), request={schema:"com.arkamax.ulanzi.imageslide.setup-request/v5",pluginVersion:VERSION,createdUtc:now.toISOString(),expiresUtc:new Date(now.getTime()+30*60e3).toISOString(),setupKey:key,setupActionIdSha256:hash(setupActionId.toLowerCase()),dataRootHash:hash(resolve(dataRoot)),store:target.store,groupId:target.groupId,pageId:target.pageId,manifestPath:target.manifest,manifestSha256:await fileHash(target.manifest),rootManifestSha256:await fileHash(target.rootManifest),profileNameSha256:hash(target.profileName.toLowerCase()),deviceUuidSha256:hash(target.deviceUuid.toLowerCase()),operation:target.kind,action:ACTION};
  if(target.kind==="restore")request.restore=await restoreCandidate(stateRoot,target);
  const requests=under(stateRoot,"requests");await mkdir(requests,{recursive:true});const file=`${randomUUID().replaceAll("-","")}.json`, text=JSON.stringify(request);await atomicWrite(under(requests,file),text);await atomicWrite(under(requests,`${file}.sha256`),`${hash(text)}\n`);await atomicWrite(under(requests,"current.json"),JSON.stringify({schema:"com.arkamax.ulanzi.imageslide.request-pointer/v1",file,sha256:hash(text)}));return request;
}
async function request(stateRoot){const root=under(stateRoot,"requests"), pointer=await json(under(root,"current.json"),"REPREPARE_REQUIRED","APPLY_PRECHECK");if(pointer?.schema!=="com.arkamax.ulanzi.imageslide.request-pointer/v1"||! /^[0-9a-f]{32}\.json$/i.test(pointer.file)||basename(pointer.file)!==pointer.file)fail("REPREPARE_REQUIRED","APPLY_PRECHECK");const text=await readFile(under(root,pointer.file),"utf8");if(hash(text)!==pointer.sha256||hash(text)!==(await readFile(under(root,`${pointer.file}.sha256`),"utf8")).trim())fail("REPREPARE_REQUIRED","APPLY_PRECHECK");const value=JSON.parse(text);if(value.schema!=="com.arkamax.ulanzi.imageslide.setup-request/v5"||value.pluginVersion!==VERSION||Date.parse(value.expiresUtc)<=Date.now())fail("REPREPARE_REQUIRED","APPLY_PRECHECK");return value}
async function inspectRestoreRun(stateRoot,runId,target,currentPage){const root=under(stateRoot,"backups",runId),backup=under(root,"manifest.before.json"),receiptPath=under(root,"receipt.json");let receipt;try{receipt=await json(receiptPath,"RESTORE_BACKUP_INVALID","RESTORE_RESOLUTION")}catch{return{status:"malformed"}}if(!receiptBound(receipt,target))return{status:"unbound"};try{const backupText=await readFile(backup,"utf8"),before=hash(backupText),backupPage=JSON.parse(backupText),backupCenter=large(backupPage,"RESTORE_BACKUP_INVALID","RESTORE_RESOLUTION").Actions["3_2"],currentCenter=large(currentPage,"RESTORE_BACKUP_INVALID","RESTORE_RESOLUTION").Actions["3_2"],fingerprint=centerFingerprint(currentCenter);if(receipt.schema!=="com.arkamax.ulanzi.imageslide.setup-receipt/v1"||receipt.operation!=="patch"||receipt.result!=="success"||receipt.action!==ACTION||receipt.beforeSha256!==before||receipt.backupSha256!==before||receipt.afterSha256===before||!fingerprint||backupCenter?.Action!==BUILTIN)return{status:"invalid"};const expected=structuredClone(backupPage);large(expected,"RESTORE_BACKUP_INVALID","RESTORE_RESOLUTION").Actions["3_2"]=generatedCenter(currentCenter.ActionID);if(hash(JSON.stringify(expected,null,2))!==receipt.afterSha256)return{status:"invalid"};if(receipt.centerActionFingerprintSha256!==undefined&&receipt.centerActionFingerprintSha256!==fingerprint)return{status:"invalid"};if(normalizedPatchedPage(expected,target.setupKey)!==normalizedPatchedPage(currentPage,target.setupKey))return{status:"invalid"};return{status:"candidate",value:{runId,receiptSha256:await fileHash(receiptPath),backupSha256:before,beforeSha256:before,afterSha256:receipt.afterSha256,centerActionFingerprintSha256:fingerprint}}}catch{return{status:"invalid"}}}
async function restoreCandidate(stateRoot,target){let runs=[];try{runs=await (await import("node:fs/promises")).readdir(under(stateRoot,"backups"),{withFileTypes:true})}catch{fail("RESTORE_BACKUP_NOT_FOUND","RESTORE_RESOLUTION")}const currentPage=await json(target.manifest,"PAGE_INVALID","RESTORE_RESOLUTION"),candidates=[];let mismatch=false;for(const run of runs.filter(value=>value.isDirectory()&&safeSegment(value.name))){const inspected=await inspectRestoreRun(stateRoot,run.name,target,currentPage);if(inspected.status==="candidate")candidates.push(inspected.value);else if(inspected.status!=="unbound")mismatch=true}if(candidates.length===0)fail(mismatch?"RESTORE_BACKUP_INVALID":"RESTORE_BACKUP_NOT_FOUND","RESTORE_RESOLUTION");if(candidates.length!==1)fail("PROFILE_AMBIGUOUS","RESTORE_RESOLUTION");return candidates[0]}
async function validateRequestedTarget(dataRoot,req){if(req.store!=="ProfilesV2"||!safeSegment(req.groupId)||!safeSegment(req.pageId))fail("PROFILE_NOT_FOUND","APPLY_PRECHECK");const rootManifest=under(dataRoot,"ProfilesV2",req.groupId,"manifest.json"), manifest=under(dataRoot,"ProfilesV2",req.groupId,"Profiles",req.pageId,"manifest.json");if(await fileHash(rootManifest)!==req.rootManifestSha256||await fileHash(manifest)!==req.manifestSha256)fail("PAGE_INVALID","APPLY_PRECHECK");const root=await json(rootManifest,"MANIFEST_INVALID","DEVICE_PROFILE_MATCH"), state=await json(under(dataRoot,"Config","setting_source.json"),"SETTINGS_SCHEMA_UNSUPPORTED","SETTINGS_READ");if(root?.Device?.Model!=="D200"||hash(String(root?.Name).toLowerCase())!==req.profileNameSha256||hash(String(root?.Device?.UUID).toLowerCase())!==req.deviceUuidSha256||root?.Pages?.Current!==req.pageId||!root?.Pages?.Pages?.includes(req.pageId)||!state?.Devices?.some(d=>hash(String(d?.CurrentProfile).toLowerCase())===req.profileNameSha256&&hash(String(d?.CurrentDevice).toLowerCase())===req.deviceUuidSha256))fail("PROFILE_NOT_FOUND","APPLY_PRECHECK");const page=await json(manifest,"MANIFEST_INVALID","APPLY_PRECHECK");setupController(page,req.setupKey,req.setupActionIdSha256);return {manifest,page}}
export async function apply({dataRoot,stateRoot,isStudioClosed=studioClosed,afterRename=async()=>{}}){
  if(!await isStudioClosed())fail("HELPER_PROCESS_FAILED","APPLY_PRECHECK");const req=await request(stateRoot);if(req.dataRootHash!==hash(resolve(dataRoot)))fail("REPREPARE_REQUIRED","APPLY_PRECHECK");const {manifest,page}=await validateRequestedTarget(dataRoot,req),current=large(page).Actions["3_2"];if(current?.Action!==(req.operation==="restore"?ACTION:BUILTIN))fail("SLOT_UNRELATED","SLOT_VALIDATION");
  let replacement;if(req.operation==="restore"){const r=req.restore;if(!r||!safeSegment(r.runId))fail("RESTORE_BACKUP_INVALID","RESTORE_RESOLUTION");const inspected=await inspectRestoreRun(stateRoot,r.runId,{...req,setupKey:req.setupKey},page),candidate=inspected.value;if(inspected.status!=="candidate"||candidate.receiptSha256!==r.receiptSha256||candidate.backupSha256!==r.backupSha256||candidate.beforeSha256!==r.beforeSha256||candidate.afterSha256!==r.afterSha256||candidate.centerActionFingerprintSha256!==r.centerActionFingerprintSha256)fail("RESTORE_BACKUP_INVALID","RESTORE_RESOLUTION");replacement=await readFile(under(stateRoot,"backups",r.runId,"manifest.before.json"),"utf8")}else{large(page).Actions["3_2"]=generatedCenter(randomUUID());replacement=JSON.stringify(page,null,2)}
  const run=under(stateRoot,"backups",`${Date.now()}-${randomUUID()}`);await mkdir(run,{recursive:true});const backup=under(run,"manifest.before.json");await cp(manifest,backup);const beforeSha256=await fileHash(backup);
  const temp=under(dirname(manifest),`.imageslide-${randomUUID()}.tmp`);await writeFile(temp,replacement,"utf8");const check=await json(temp,"MANIFEST_INVALID","READBACK"), expected=req.operation==="restore"?BUILTIN:ACTION;if(large(check,"MANIFEST_INVALID","READBACK").Actions["3_2"]?.Action!==expected||(expected===ACTION&&!normalizedCenter(large(check,"MANIFEST_INVALID","READBACK").Actions["3_2"]))){await rm(temp,{force:true});fail("MANIFEST_INVALID","READBACK")}await rename(temp,manifest);try{await afterRename();const after=await json(manifest,"MANIFEST_INVALID","READBACK");if(large(after,"MANIFEST_INVALID","READBACK").Actions["3_2"]?.Action!==expected)fail("MANIFEST_INVALID","READBACK");const receipt={schema:"com.arkamax.ulanzi.imageslide.setup-receipt/v1",operation:req.operation,result:req.operation==="restore"?"restored":"success",target:{store:"ProfilesV2",groupId:req.groupId,pageId:req.pageId,key:"3_2"},beforeSha256,afterSha256:await fileHash(manifest),backupSha256:await fileHash(backup),action:ACTION,timestampUtc:new Date().toISOString()};if(req.operation==="patch")receipt.centerActionFingerprintSha256=centerFingerprint(large(after).Actions["3_2"]);await atomicWrite(under(run,"receipt.json"),JSON.stringify(receipt));return receipt}catch(error){const rollback=under(dirname(manifest),`.imageslide-${randomUUID()}.rollback`);try{await cp(backup,rollback);await rename(rollback,manifest);if(await fileHash(manifest)!==beforeSha256)fail("HELPER_PROCESS_FAILED",req.operation==="restore"?"RESTORE_WRITE":"PATCH_WRITE")}catch{await rm(rollback,{force:true});fail("HELPER_PROCESS_FAILED",req.operation==="restore"?"RESTORE_WRITE":"PATCH_WRITE")}throw error}
}
function args(values){const result={};for(let i=0;i<values.length;i+=2)result[values[i]?.replace(/^--/,"")]=values[i+1];const key=result["pressed-key"],actionId=result["setup-action-id"];statusBinding=/^[0-9]{1,2}_[0-9]{1,2}$/.test(key)&&key!=="3_2"&&uuid(actionId)?{setupKey:key,setupActionIdSha256:hash(actionId.toLowerCase())}:{};return result}
export async function main(argv=process.argv.slice(2),{readBundleIdentifier=async()=>{const {stdout}=await execFile("defaults",["read","/Applications/Ulanzi Studio.app/Contents/Info","CFBundleIdentifier"]);return stdout.trim()},isStudioClosed=studioClosed,now=Date.now,wait=()=>new Promise(done=>setTimeout(done,500))}={}){const a=args(argv),stateRoot=a["state-root"]||join(homedir(),"Library","Application Support","Arkamax","ImageSlidePlugin"),dataRoot=a["data-root"]||join(homedir(),"Library","Application Support","Ulanzi","UlanziDeck"),handshakeId=a["handshake-id"]||randomUUID();try{await writeStatus(stateRoot,"started","PREPARING","INITIALIZING",handshakeId);const pluginRoot=a["plugin-root"];if(!pluginRoot)fail("COMPATIBILITY_UNSUPPORTED","COMPAT_PLUGIN_ROOT");await validateStudioIdentity(pluginRoot,{bundleIdentifier:await readBundleIdentifier()});if(a.mode==="assistant"){await prepare({dataRoot,stateRoot,key:a["pressed-key"],setupActionId:a["setup-action-id"],operation:a.operation});await writeStatus(stateRoot,"prepared","PREPARED","REQUEST_WRITE",handshakeId);const deadline=now()+900000;while(!await isStudioClosed()){if(now()>=deadline)fail("HELPER_PROCESS_FAILED","APPLY_PRECHECK");await wait()}const receipt=await apply({dataRoot,stateRoot,isStudioClosed});const code=receipt.result==="restored"?"RESTORED":"SUCCESS";await writeStatus(stateRoot,receipt.result,code,"RELAUNCH",handshakeId);process.stdout.write(`IMAGESLIDE_DIAGNOSTIC:${code}:RELAUNCH\n`)}else if(a.mode==="prepare"){await prepare({dataRoot,stateRoot,key:a["pressed-key"],setupActionId:a["setup-action-id"],operation:a.operation});await writeStatus(stateRoot,"prepared","PREPARED","REQUEST_WRITE",handshakeId);process.stdout.write("IMAGESLIDE_DIAGNOSTIC:PREPARED:REQUEST_WRITE\n")}else if(a.mode==="apply"){const receipt=await apply({dataRoot,stateRoot,isStudioClosed});const code=receipt.result==="restored"?"RESTORED":"SUCCESS";await writeStatus(stateRoot,receipt.result,code,"RELAUNCH",handshakeId);process.stdout.write(`IMAGESLIDE_DIAGNOSTIC:${code}:RELAUNCH\n`)}else fail("HELPER_PROCESS_FAILED","INITIALIZING");return true}catch(error){const code=CODES.has(error.code)?error.code:"HELPER_PROCESS_FAILED",phase=PHASES.has(error.phase)?error.phase:"INITIALIZING";try{await writeStatus(stateRoot,"failed",code,phase,handshakeId)}catch{}process.stderr.write(`IMAGESLIDE_DIAGNOSTIC:${code}:${phase}\n`);return false}}
if(process.argv[1]&&import.meta.url===pathToFileURL(process.argv[1]).href)process.exitCode=await main()?0:1;
